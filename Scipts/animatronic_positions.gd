extends Node


func _ready() -> void:
	pass
	#print_children_recursive(self)
#
#func print_children_recursive(node: Node, indent := "") -> void:
	#for child in node.get_children():
		#print(indent + child.name)
		#print_children_recursive(child, indent + "  ")
